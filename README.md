# Gestion scolaire Django

Installation Windows: créer venv, `pip install -r requirements.txt`, `cd gestion_ecole`, `python manage.py migrate`, `python manage.py runserver`.

Modules inclus: élèves, classes, matières, évaluations, présences, finances, factures, rapports.
